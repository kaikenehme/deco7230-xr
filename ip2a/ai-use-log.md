# IP2a — Running AI-use log

**Purpose:** IP2a is not formally graded, but its testing plan and results are submitted as
usual and feed Evaluation 2; IP2b is a secure assessment. The same rule is kept from IP1:
every AI use is logged as it happens, then consolidated into the UQ Library acknowledgement
table before the Week 9 session. Under-declaring is the risk; log everything.

**Tool used:** Claude Code (Anthropic, model Fable 5.1), driven interactively by Kaike Nehme.

| Date | What AI did | Files touched | Human role |
|---|---|---|---|
| 2026-09-03 | Extracted the IP1 findings and Evaluation 1 §05–06 commitments, mapped the Unity codebase and Poly Haven asset availability, and wrote the IP2a build plan (17 tasks: entry affordance, room 9×7 m, window + six-stop time of day via wall clock, lamp pull cord, three preset rooms, ~24 furniture, place-at-ray + stick rotate + outline, onboarding, harmony thread, instrument fixes) | `docs/superpowers/plans/2026-09-03-ip2a-build.md` | Kaike chose the four additions (bigger room, presets, window/sun, more furniture) and the four design decisions (move project, framed-picture picker, discrete clock stops, cut scheme cycling from the test) |
| 2026-09-03 | Moved the Unity project `ip1/RenovationPreviewer` → `ip2a/RenovationPreviewer` (git rename, IP1 frozen as tag `ip1-submitted`), build output renamed to `Builds/ip2a.apk`, pointer file + README/CLAUDE.md updated, this log started | `ip2a/RenovationPreviewer/**`, `ip1/RenovationPreviewer.md`, `README.md`, `CLAUDE.md`, `ip2a/ai-use-log.md` | Kaike asked for the move rather than a copy |
| 2026-09-03 | Implemented the entry affordance on kept surfaces (`PullAffordance` swatch tab at the touch point, `HandGlow` on the controllers, sample peels from the tab via XRI attach easing, pull works within 35 cm not only on contact, menu trigger yields to the pull), facilitator sample-in-hand (left X held 1 s), lamp rewrite (bulb-only Warm/Cool/Off, pull cord, idle pulse, applied at Start), six-stop time of day (`TimeOfDay`, `TimeOfDayController` owning sun/ambient/sky, soft shadows, procedural skybox, ambient-mode bug fixed); EditMode 99/99, PlayMode 12/12 | `Assets/Scripts/Runtime/{PullAffordance,HandGlow,FacilitatorSpawn,SamplePuller,MarkTool,MenuSelectRelay,LampController,TimeOfDay,TimeOfDayController}.cs`, `Assets/Editor/{SceneBuilder,ProjectConfigurator}.cs`, tests | Design comes from Evaluation 1 §05 (Kaike's iteration) and the 3 Sep decisions; Kaike verifies feel in the simulator |
| 2026-09-03 | Window in Wall_S (one logical Surface of four cubes, paintable as one wall), window frame, glass, outside ground; primitive wall clock that advances the time of day on touch; catalogue grown to 24 CC0 Poly Haven pieces in five categories ordered so menu pages = categories, sample colours averaged from the textures; EditMode 103/103, PlayMode 14/14 | `Assets/Editor/{SceneBuilder,CatalogueImporter}.cs`, `Assets/Scripts/Runtime/{WallClock,Catalogue}.cs`, `Assets/Catalogue/**` (generated), tests | Kaike asked for the window/sun and more furniture; AI chose the ids from the live Poly Haven list |
| 2026-09-03 | Furniture manipulation (XRI pose tracking off; held piece sits where the ray meets the floor or under the hand, thumbstick of the holding hand yaws it, all locomotion providers locked while holding, release snaps upright), inverted-hull outline on hover/hold instead of the tinted glow, three preset rooms generated from code (`PresetAuthoring` → `Assets/Presets`, `PresetApplier` at Start, framed pictures on Wall_W with procedural cards), first drafts of the IP2a testing plan, data sheet, results scaffold and the Quest sideload runbook; EditMode 122/122, PlayMode 18/18 | `Assets/Scripts/Runtime/{FurnitureSlot,LocomotionLock,FurnitureInput,SelectionOutline,RenovationConfig,RoomPreset,PresetApplier,PresetFrame,PresetCard}.cs`, `Assets/Editor/{PresetAuthoring,SceneBuilder}.cs`, `Assets/Presets/**`, tests, `ip2a/*.md`, `testing-data/ip2a/README.md` | Preset contents (which floor, paints, pieces, where) are AI's first pass for Kaike to re-dress; testing-plan wording follows Kaike's Evaluation 1 iteration and needs their voice pass |
| 2026-09-03 | Ten-second onboarding (kept surfaces pulse under a "staying" label, lamp pulses), harmony thread + source swatch on the held sample, per-surface material instance cache (no allocation per hover), IP2a data-entry page; added a headless snapshot test that renders 14 viewpoints to `Builds/snapshots/` so the room could be checked without the editor; from those renders fixed mirrored labels, caption size, chair yaws, storage placement, thread width, lamp/ambient balance, and switched sample colours from texture mean to dominant colour; EditMode 128/128, PlayMode 21/21 | `Assets/Scripts/Runtime/{OnboardingSequence,HarmonyThread,Surface,PullAffordance,LampController,TimeOfDay}.cs`, `Assets/Editor/{SceneBuilder,PresetAuthoring,CatalogueImporter}.cs`, `Assets/Scripts/PlayModeTests/SnapshotTests.cs`, `testing-data/ip2a/data-entry.html`, tests | Kaike to run the simulator for feel (grip, trigger, thumbstick rotate, twist) and re-dress the presets to taste |
| 2026-09-11 | Simulator feel-pass tooling: editor-only gaze aim (`EditorGazeAim`, right controller rides the head so mouse-look aims the ray, scroll wheel = twist, F4 toggles back to the raw XR Device Simulator, inert on device); menu now also closes on right B (same hand that opened it) and Backspace in the editor, `SchemeCycler` yields to an open menu; EditMode 137/137, PlayMode 22/22; drove the live editor through the Unity CLI (open, recompile, tests, `SceneBuilder.Build`, Play) | `Assets/Scripts/Runtime/{EditorGazeAim,MenuSelectRelay,SchemeCycler,ControllerMenu}.cs`, `Assets/Editor/SceneBuilder.cs`, tests | Kaike asked for mouse-driven aiming and a menu-close shortcut; AI interrogated both (right B on device rather than a new key only) and Kaike approved before build |
| 2026-09-22 | Simulator feel pass, round 1 (Kaike in the editor, AI drove the live editor through the Unity CLI and fixed what was reported, in order): both controllers now visible (left hand rides the head as a follower; hand offset moved from 41° to 19° below the eye line so the models sit inside the Game view FOV); surface hover shows an amber rectangle on the hit face (`SurfaceFrame`, LineRenderer on the bounds face) instead of lighting the whole wall, and the menu's current target keeps its frame/outline instead of the whole-object tint; controller menu moved to 26 cm up / 16 cm ahead of the left hand, clear of the model; gaze aim now respects the simulator's own T / Y / Tab hand targeting instead of forcing the right hand every frame (`Reconcile`); Pipeline package 0.5.0 → 0.7.0 (the old one quit the editor on `editor_stop`); EditMode 149/149 | `Assets/Scripts/Runtime/{EditorGazeAim,SurfaceFrame,RayFeedback}.cs`, `Assets/Editor/SceneBuilder.cs`, tests, `Packages/manifest.json` | Kaike reported each issue from the simulator ("controllers not displayed", "frame highlight not the whole object", "menu further from the left controller", "T/Y not working", "floor highlighted all the time"); AI diagnosed with in-editor probes and screenshots before each change. T/Y turned out to be Game view focus, not code; the earlier own-key polling was removed in favour of the simulator's handlers |
| 2026-09-22 | Simulator keys (T / Y / G) dead twice in the feel pass; a raw-event key logger injected into play mode showed the keys never reached Unity unless the Game view itself had focus (Input System default `PointersAndKeyboardsRespectGameViewFocus`). Added an Input System settings asset via `ProjectConfigurator.SetupInputForSimulator` with `AllDeviceInputAlwaysGoesToGameView`; keys verified arriving with no click | `Assets/Editor/ProjectConfigurator.cs`, `Assets/Settings/InputSystem.inputsettings.asset` | Editor-only behaviour, no effect on device |
| 2026-09-23 | Furniture grab bug ("still bugged" after keys were confirmed arriving). Headless PlayMode probe in the real Room scene: the right NearFarInteractor aimed at the Scandi armchair sees it as a valid target, a select marks it held and it follows the ray — mechanics were fine. The probe showed the actual fault: on grab the chair jumped 1.3 m, from (0.60, −1.30) to (1.14, −2.47), because the slot snapped to where the ray met the floor *behind* the chair; a near-level ray fell back to 2 m from the hand, so pieces also jumped toward the player. Fix: keep the piece's offset from the ray/hand target at grab time (re-measured on a near↔far switch), and cap the ray's floor point at 6 m horizontal reach so crossing the horizon is continuous. The probe became `FurnitureGrabTests` (grab = no move, re-aim moves by the ray's delta, release flat); EditMode 152/152, PlayMode 23/23 (T15 sweep) | `Assets/Scripts/Runtime/FurnitureSlot.cs`, `Assets/Scripts/Tests/FurnitureSlotTests.cs`, `Assets/Scripts/PlayModeTests/{FurnitureGrabTests.cs,Renovation.PlayModeTests.asmdef}` | The select itself was forced in the test; a real `G` press on a chair in the simulator still needs Kaike's confirmation |
| 2026-09-23 | Preset-room review from the headless renders. Bright lines on Wall_W and an L on the floor were sunlight through open 10 cm notches where the walls met the flush W × D floor/ceiling slabs; the slabs now run to the walls' outer faces (interior still 9 × 7 m, asserted wall face to wall face). The floor's "staying" label, which floated mid-room over the coffee table, is gone at Kaike's request — the floor still pulses, kept furniture keeps its label. Re-rendered: lines gone, a faint hairline at the Wall_W/ceiling corner remains. EditMode 152/152, PlayMode 24/24 | `Assets/Editor/SceneBuilder.cs`, `Assets/Scenes/Room.unity` (regenerated), `Assets/Scripts/Runtime/OnboardingSequence.cs`, tests | Kaike asked for both; preset re-dress (style, spread) left as Kaike's call |
| 2026-09-23 | Preset rooms re-laid out as living rooms at Kaike's request (furniture had been clustered in the middle). AI looked up standard spacing (sofa to coffee table 36–46 cm, walkways 76–91 cm; Livingetc, OROA) and placed each preset as zones: lounge on the window wall with a pair of chairs angled in, reading nook beside the lamp, storage on Wall_E/Wall_N, a table-for-two, plants in corners (5 → 12–14 pieces). New PlayMode test checks, per preset: no overlapping pieces, start spot / preset frames / door / lamp kept clear, nothing through a wall, furniture spans ≥ 70 % of the room. That test exposed a real bug: `FurnitureSlot.FitCollider` used the world AABB as the local box size, so any piece turned 90° had a transposed collider (Shelf_01's box stuck 1 m out from the wall) and angled pieces had inflated ones — which also affects ray hits and grabs. Now measured in slot space. EditMode 154/154, PlayMode 25/25; renders checked | `Assets/Editor/PresetAuthoring.cs`, `Assets/Presets/*.asset` (regenerated), `Assets/Scripts/Runtime/FurnitureSlot.cs`, tests | Placement is AI's; the pieces' styles (ornate Scandi sofa) unchanged — Kaike to judge |
| 2026-09-23 | Simulator aiming + menu distance (Kaike: hard to select things, menu too close to the left controller). Two causes. (1) The simulated right hand rode the head 20 cm right / 13 cm below the eye with its ray parallel to the gaze, so the ray never landed where Kaike looked (≈ 20 cm off at menu distance). Editor-only fix: the aiming hand now converges on the gaze point (`EditorGazeAim.Converge` + `GazePoint`, skips the rig and anything held so a far-held piece cannot steer its own ray); verified live, ray 0.00° off the gaze hit. (2) The menu's lift never applied: a Canvas RectTransform serializes `anchoredPosition`, so SceneBuilder's `localPosition` y (0.26 m on 22 Sep) was dropped on save. Now set via `anchoredPosition3D` after the component is added; moved to 16 cm up / 40 cm ahead of the left hand (~0.8 m from the eye, eye level in the simulator), wiring test added. Grab test's re-aim pointed toward room centre (new layout picked the corner chair). EditMode 157/157, PlayMode 26/26 (live editor) | `Assets/Scripts/Runtime/EditorGazeAim.cs`, `Assets/Editor/SceneBuilder.cs`, `Assets/Scenes/Room.unity` (regenerated), tests | Menu offset also applies on device (40 cm out from the real hand) — check on the Quest Friday |
| 2026-09-23 | Desktop (mouse) test mode after Kaike decided IP2a runs without a headset: `EditorDesktopRig` — free cursor aims the right-hand ray, right-drag looks, WASD walks inside the room, simulator still supplies click/G; cursor lock stands down while it is on. Committed as untested WIP: the live editor wedged when AI triggered a recompile during Kaike's Play session (AI's error — it saw `isPlaying = true` and carried on), leaving XRI's poke-raycaster registry out of sync (`KeyNotFoundException` on the menu) and a 13 GB log, which AI truncated. Verification deferred to 24 Sep | `Assets/Scripts/Runtime/{EditorDesktopRig,EditorGazeAim,SimulatorCursorLock}.cs`, `Assets/Editor/SceneBuilder.cs`, tests | Not yet run; scene not rebuilt |
| 2026-09-24 | Desktop-mode WIP verified headless: scene regenerated with `SceneBuilder.Build`, EditMode 161/161, PlayMode 27/27 (incl. `EditorDesktopRigTests`, `DesktopMode_OnInEditor_CursorFree_HeadDriverOff`). Live check in the editor (cursor aim, menu click, right-drag look, WASD) still to do with Kaike | `Assets/Scenes/Room.unity` (regenerated) | Headless tests only; not yet tried by hand |
| 2026-09-24 | Desktop mode, first hands-on lap (Kaike). (0) Nothing rendered: the editor had opened an untitled scene, not `Room.unity` — reopened. (1) "Can't turn the head": an in-memory probe showed right-drag did turn, but ~90° per full screen width and it stopped at the screen edge; the cursor now locks and hides while the right button is held, sensitivity 0.15 → 0.3°/px. (2) Menu unreadable at ~0.8 m: desktop mode pulls it to 0.45 m from the eye (device offset restored when desktop mode is off); checked by screenshot. (3) Preset frames not selectable: frames, lamp and clock were touch-only and the head-riding hands can't reach a wall; new `ITouchable`, and in desktop mode a left click on one touches it (nearest hit, blocked by walls, rig skipped). Kaike re-ran the lap: all working. EditMode 165/165, PlayMode 27/27 | `Assets/Scripts/Runtime/{EditorDesktopRig,ITouchable,PresetFrame,LampController,WallClock}.cs`, `Assets/Editor/SceneBuilder.cs`, `Assets/Scenes/Room.unity` (regenerated), tests | Click-to-touch is editor-only; on device the props are still touched by hand |
| 2026-09-24 | Bedroom preset at Kaike's request; Kaike chose to replace Coastal and keep the bed. AI searched Poly Haven: of three beds only `GothicBed_01` has a mattress (the others are a bare metal frame and a day bed). Added it and `drawer_cabinet` (chest of drawers) to the CC0 catalogue (new `FurnitureCategory.Bed`, appended so existing menu pages don't shift), credits updated. Bedroom: carpet + bed kept, Warm Neutral walls, Tranquil Retreat behind the bed; headboard on the window wall under the clock so the kept bed is in view from the start spot; nightstands either side, bench at the foot, reading chair by the lamp, drawers on Wall_E, wardrobe + shelves on Wall_N, two plants (11 pieces). Layout test (no overlaps, start/door/lamp/frames clear, spread) passes; checked in two renders. EditMode 165/165, PlayMode 27/27 | `Assets/Editor/{CatalogueImporter,PresetAuthoring}.cs`, `Assets/Scripts/Runtime/Catalogue.cs`, `Assets/Catalogue/*` (imported), `Assets/Presets/Bedroom*` (Coastal removed), `Assets/Scenes/Room.unity` (regenerated) | Placement and colours are AI's; Kaike to judge in the editor. Default preset (Scandi) unchanged, so the protocol's "floor and sofa are staying" still holds for Task 1 |
| 2026-09-24 | iPad session logger at Kaike's request (no printed sheets): a claude.ai artifact with the data sheet's 21 rows as tap targets, phase timers (45 s / 2 min / 2 min / 1:30), a Task 1 stopwatch for row 5, a numbered prompt log that records the phase, branching 17a/17b on row 8, and a participant controls card; saves to the artifact's database, with a copy on the iPad if the wifi drops. Scripted prompts reworded for desktop mode (click, W A S D, scroll, G). Test plan updated to match: desktop platform, Bedroom preset, iPad logging, limitation stated (no reach, scale or hold-up distance in desktop mode) | `ip2a/ipad-logger.html`, `ip2a/2026-09-25-ip2a-testing-plan.md`, `ip2a/data-collection-sheet.md` | Prompt wording is AI's; Kaike's voice pass still to do. Rotate prompt pending: how held furniture turns in desktop mode is unconfirmed |
| 2026-09-24 | Bug Kaike found: with the menu open in front of a preset frame, clicking a paint swatch also switched the room. Cause was AI's own click-to-touch picker from earlier today: it skipped everything under the rig to ignore the hands, and the menu hangs off the left hand, so the click passed through the panel (the existing `RayUtil` already special-cases the `MenuPanel` tag; the picker didn't). Fixed, test first. Found next to it: `Hide()` only switched the canvas off, so a closed menu's collider stayed in place as an invisible wall — in desktop mode 0.45 m in front of the eye, swallowing clicks aimed through that patch. The collider now follows the menu's visibility. EditMode 167/167, PlayMode 27/27 | `Assets/Scripts/Runtime/{EditorDesktopRig,ControllerMenu}.cs`, tests | Hidden-collider bug predates today but only mattered once the desktop menu sat in the line of sight; on device it also blocked rays near the left hand |
| 2026-09-24 | Kaike: holding W brought up teleport. Cause (simulator source, `ProcessAxis2DControlInput`): the XR Device Simulator copies W/A/S/D into the controllers' thumbsticks, and thumbstick-forward is XRI's teleport aim — so W both walked (desktop rig) and aimed a teleport. Desktop mode now sets the simulator's `axis2DTargets` to None (restored on F4). That also removed the only way to turn held furniture in the editor (A/D had been pushing the stick sideways), so Q/E now turn a held piece in desktop mode; the device path (thumbstick) is unchanged. iPad logger prompt 7 and controls card updated. EditMode 168/168, PlayMode 27/27; live check: simulator feed `None` in Play | `Assets/Scripts/Runtime/{EditorDesktopRig,FurnitureInput}.cs`, tests, `ip2a/ipad-logger.html`, `ip2a/data-collection-sheet.md` | Q/E choice is AI's (Q/E were the simulator's up/down, unused in desktop mode); checked by Kaike 24 Sep |
| 2026-09-24 | Kaike: clicking the clock or lamp also opened a menu. One click did two things: desktop click-to-touch touched the prop, and the simulator's trigger reached `MenuSelectRelay`, whose `RayUtil.TryHit` skips trigger colliders (lamp, clock and frames are trigger-only), so the ray fell through to the wall/floor behind and opened its menu. The relay now does nothing when a touchable prop is first on the ray; the picker moved from `EditorDesktopRig` to `RayUtil.PickTouchable` since device code now uses it. Also applies on the headset (pointing at the lamp no longer opens the wall behind). Prop zones are small (lamp 27 cm, clock 22 cm, frames 30 cm). EditMode 169/169, PlayMode 27/27 | `Assets/Scripts/Runtime/{MenuSelectRelay,RayUtil,EditorDesktopRig}.cs`, tests | Checked by Kaike 24 Sep |
| 2026-09-24 | Pre-test check against the IP2 brief at Kaike's request. Plan brought in line with desktop mode: a headset → desktop controls table, facilitator key (T + hold B, not X), recording tool, 26 pieces / four menu pages, and a wrong post-test cross-reference (said "row 10, branched by row 14"; is 17a/17b branched on row 8). Blackboard PDF built from the markdown in the IP1 print style (`build-plan-pdf.py`, Chrome headless) so it can be rebuilt after Kaike's voice pass | `ip2a/2026-09-25-ip2a-testing-plan.md`, `ip2a/data-collection-sheet.md`, `ip2a/build-plan-pdf.py`, `ip2a/testing-plan-print.html`, `ip2a/IP2a-Testing-Plan-Kaike-Nehme.pdf` | Wording edits are factual only; Kaike's voice pass still to do, then rebuild the PDF |
| 2026-09-25 | Change of plan on test day (Kaike): run IP2a on the Quest 3S. AI built the APK headless (`BuildScript.BuildAndroid`, 3 min 30 s, 0 errors), installed and launched it over adb, read logcat (72/72 fps, no Unity errors), and set up screen mirroring with scrcpy (Homebrew, open source) using Unity's adb. AI declined to open the supplied QuestLiveView DMG itself (unsigned, rejected by Gatekeeper) and told Kaike why | `ip2a/RenovationPreviewer/Builds/ip2a.apk` (gitignored), `ip2a/quest-sideload.md` | Kaike authorised USB debugging in the headset; the decision to switch to device is Kaike's |

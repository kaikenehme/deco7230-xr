# Code walkthrough — IP2a (for questions in the studio)

Where each feature lives, and what changed since IP1. Line numbers are as of `1860a94` (24 Sep); if a line has moved, search for the method name.

**Paths:** `R/` = `ip2a/RenovationPreviewer/Assets/Scripts/Runtime/` · `E/` = `ip2a/RenovationPreviewer/Assets/Editor/` · `T/` = `ip2a/RenovationPreviewer/Assets/Scripts/Tests/` (+ `PlayModeTests/`). IP1 as tested = git tag `ip1-submitted`.

## The 30-second answer

- **The scene is generated by code, not built by hand.** `E/SceneBuilder.cs` `Build()` (line 29) creates the room, window, lamp, clock, preset frames, the XR rig and wires every script. `Room.unity` is its output; I never edit it directly. Same for the three rooms (`E/PresetAuthoring.cs`) and the furniture/paint/material catalogue (`E/CatalogueImporter.cs`, CC0 assets from Poly Haven and ambientCG, Dulux colour names).
- **Surfaces and samples never know about each other** (concept spec §8). `Surface` holds a wall's state and colour; `Sample` holds a pulled colour; `HoldUpPreviewer` is the only script that connects them. That keeps the hold-up loop and the menu independent: both just call `Surface.Preview` / `Commit`.
- **No floating menus for the core loop.** Lamp, clock and preset frames are objects you touch in the room. The controller menu is the one panel, and it exists for the A3 comparison (constrained hold-up vs open catalogue).
- **Tested:** 33 test files. EditMode 169 + PlayMode 27, all passing. PlayMode tests load the real generated scene and check the wiring (`T/../PlayModeTests/SceneWiringTests.cs`), including a furniture-layout check for every preset room.
- **Size:** about 3,200 lines of runtime C# across 43 scripts.

## The core loop (from IP1, still the centre)

| Step | What the user does | Where |
|---|---|---|
| **Mark** | Sweep a controller across a surface holding grip → keep/change | `R/MarkTool.cs` `OnTriggerStay` (41) → `R/Surface.cs` `ToggleState` (94) |
| **Pull** | Trigger near a kept surface → sample in hand | `R/SamplePuller.cs` `Update` (39), `SpawnFrom` (54). Refuses change surfaces: only kept things give samples |
| **Hold** | Bring the sample near a changeable wall → whole wall previews | `R/HoldUpPreviewer.cs` `Update` (39): nearest change surface within `PreviewRadius` 0.45 m; leaving range reverts (57) |
| **Tune** | Twist the wrist → steps through 7 harmonised colours | `R/HarmonyTuner.cs` `RollToIndex` (19), `Tick` (25) · colours from `R/HarmonyPalette.cs` `Generate` (13): analogous, tonal, triadic, complementary |
| **Commit** | Release against the wall → applied; release mid-air → discarded | `R/HoldUpPreviewer.cs` `OnReleased` (63): `Commit` (68) or `Destroy` (71) |
| **Surface state** | Keep/Change, committed vs previewed colour and material | `R/Surface.cs` `Preview` (142), `Commit` (149), `PreviewMaterial` (159), `CommitMaterial` (166) |

## What changed since IP1, and why

Every IP2a change traces back to an IP1 finding or a participant request (Evaluation 1 §05–06).

| Change | Why (IP1 evidence) | Where |
|---|---|---|
| **First-contact feedback**: a swatch tab appears where your hand meets a kept surface, and the controller glows | 0/5 pulled a sample; all 5 touched a kept surface and got nothing back | **New** `R/PullAffordance.cs` (`NearestPullable` 54, `Update` 91) · **new** `R/HandGlow.cs` `Update` (21) · `R/SamplePuller.cs` now pulls within the tab radius, not only on contact |
| **Task 1 starts with a sample already in hand** | A2 (the main question) was never reached in IP1 because nobody got past the pull | **New** `R/FacilitatorSpawn.cs` `Update` (25): hold left X 1 s (desktop: `T`, then hold `B`) |
| **Thread from the sample back to its source** | A2 risk: the constraint was invisible, so it could feel random | **New** `R/HarmonyThread.cs` `LateUpdate` (45) |
| **Three preset rooms** (Scandi, Mid-century, Bedroom), switched by touching framed pictures | Participants asked to rearrange a furnished room, not furnish an empty one | **New** `R/RoomPreset.cs` (data), `R/PresetApplier.cs` `Apply` (26), `R/PresetFrame.cs` `Touch` (31), `R/PresetCard.cs` (frame pictures) · layouts in `E/PresetAuthoring.cs` (Scandi 25, Mid-century 49, Bedroom 70) |
| **Time of day through a window**: six stops (07:00–22:00), stepped by touching the wall clock | Q7: are three lighting states enough? Light was the concept's third reason and only had the lamp | **New** `R/TimeOfDay.cs` (`Stops` 21), `R/TimeOfDayController.cs` `Apply` (32), `R/WallClock.cs` `Touch` (34) · window built in `E/SceneBuilder.cs` |
| **Lamp is now bulb-only** (warm / cool / off), with a pulse until first touched | Daylight moved to the window so the two don't fight; lamp found 0/2 in the studio | `R/LampController.cs` `Touch` (48), `Apply` (56) |
| **Onboarding**: kept surfaces pulse under a "staying" label, then the lamp | "Diegetic isn't the same as discoverable" (Eval 1 §05) | **New** `R/OnboardingSequence.cs` `Run` (46) |
| **Furniture move**: grab, it follows the ray along the floor, rotate while held, release places | Participants' requests in IP1 (P1–P4: placement, rotate, outline) | `R/FurnitureSlot.cs`: `OnGrabbed` (137), `Update` (163), `FloorPointOnRay` (189), `GrabOffset` (199) · rotate input `R/FurnitureInput.cs` (11) · **new** `R/LocomotionLock.cs` (thumbstick means rotate while holding, not walk) |
| **Outline on furniture, frame on walls** instead of a whole-object glow | P1, P3 asked for "a frame, not the whole thing" | **New** `R/SelectionOutline.cs` · **new** `R/SurfaceFrame.cs` `Show` (68) |
| **More furniture** (26 pieces, 4 menu pages incl. beds) | Participants asked for more to choose from | `E/CatalogueImporter.cs` list at 26 · `R/Catalogue.cs` (`FurnitureCategory.Bed`) |
| **Compound surfaces**: one wall made of several pieces (the window wall) | Needed for the window | `R/Surface.cs`: renderers/colliders gathered from children; `SetBaseLook` for presets |
| **Scheme save/cycle cut from the script** | Found by 0/4 in IP1 | Still works: `R/SchemeCycler.cs`; the protocol just doesn't mention it |

## Desktop mode (editor only, for this test)

IP2a runs in the Unity editor with mouse and keyboard. None of this runs on a headset: the desktop scripts switch themselves off outside the editor (`Application.isEditor` / `#if UNITY_EDITOR`), and the Q/E path only runs while desktop mode is on.

| What | Where |
|---|---|
| Mouse aims the right-hand ray; right-drag looks (cursor locks); WASD walks, kept in the room | `R/EditorDesktopRig.cs`: `TryCursorRay` (43), `Look` (57), `Walk` (62), `Update` |
| Click on lamp / clock / frame = touching it (hands can't reach a wall here) | **New** `R/ITouchable.cs` · `R/RayUtil.cs` `PickTouchable` · called from `EditorDesktopRig.Update` |
| Clicking those props doesn't also open the wall menu behind them | `R/MenuSelectRelay.cs` `Update` (44) |
| Menu 0.45 m from the eye in desktop mode (0.8 m was unreadable) | `R/EditorDesktopRig.cs` `MenuOffset` (32), applied in `Apply` (80) |
| W no longer aims a teleport; Q/E turn held furniture | `R/EditorDesktopRig.cs` (`axis2DTargets` = None) · `TurnKeys` (54) · `R/FurnitureInput.cs` |
| Hands ride the head so they're on screen | `R/EditorGazeAim.cs` (`Compute` 66, `Converge` 76) |
| Keys reach the game even when another editor panel has focus | `E/ProjectConfigurator.cs` |

## If the professor asks…

| Question | Answer, and where to point |
|---|---|
| "How does the colour harmony work?" | `R/HarmonyPalette.cs` `Generate` (13): from the sample's hue, 7 options ordered safe → bold (analogous, tonal, triadic, complementary). The wrist angle picks one: `R/HarmonyTuner.cs` `RollToIndex` (19) |
| "How do you stop a preview sticking?" | `R/HoldUpPreviewer.cs` 57: leaving range always calls `Revert`. Only a release inside range commits (68) |
| "Why is the scene generated?" | Repeatable and reviewable: one script rebuilds the room exactly, and the tests check what it produces. `E/SceneBuilder.cs` `Build` (29) |
| "How does grabbing furniture work?" | XRI `XRGrabInteractable`; on grab the piece keeps its offset from where the ray meets the floor, capped at 6 m reach. `R/FurnitureSlot.cs` 137–199 |
| "How do you know it works?" | 169 EditMode + 27 PlayMode tests; PlayMode loads the real scene. E.g. `SceneWiringTests.Presets_Layout_NoOverlap_KeyPlacesClear_Spread` checks every room: nothing overlaps and the start spot, door and lamp are clear |
| "What about the headset?" | The same code runs on Quest: touch uses the controller's trigger collider (`OnTriggerEnter` in `PresetFrame`, `LampController`, `WallClock`). Desktop mode is editor-only. IP2b moves to the Quest in passthrough (MR) |
| "Where's the AI use?" | `ip2a/ai-use-log.md`: one row per change, what AI did and what I decided |
